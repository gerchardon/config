# -*-Shell-script-*-

function git_clone_pull() {
  debug "Clone $repo to $dir"
  repo=$1
  dir=$2
  [ ! -d "$dir" ] && {
    mkdir -p "$repo/.."
    git clone --recursive "$repo" "$dir"
  } || {
    git -C "$dir" pull
  }
}
